{
  "name": "@heva/catalog",
  "version": "0.0.0",
  "private": true,
  "description": "Shared service add-on (upsell) catalog — the single source of truth for cross-sells, consumed by apps/web (Astro) and apps/app (Next).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./orders": "./src/orders.ts"
  },
  "files": ["src"]
}
