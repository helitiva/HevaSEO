# STATUS.md — Bảng điều phối backend HevaSEO

> **Cập nhật:** 2026-06-28 · **Nguồn:** [ORCHESTRATION.md](ORCHESTRATION.md) (lane) + [ADR-backend.md](ADR-backend.md) §5 (phase).
> Đây là **single source of truth** cho fleet: slice nào ở đâu, ai làm, kẹt gì. Orchestrator + mỗi agent cập nhật hàng của mình.

**Trạng thái:** ⬜ chưa bắt đầu · 🟡 đang làm · 🔵 PR mở (chờ gác) · ✅ merged · 🔴 kẹt (xem Blocked-on)
**Gác (human):** ① chọn option · ② duyệt cổng · ③ liếc bảo mật · — không cần

---

## FOUNDATION — tuần tự (chặn fleet tới khi `pnpm verify:db` xanh)

| Epic | Slice | Trạng thái | Owner | Gác | Blocked-on / Ghi chú |
|---|---|---|---|---|---|
| **E0a** | Supabase CLI + migration pipeline + pgTAP loop | ✅ | claude | ② | **DONE 2026-06-29**: `verify:db` xanh (pgTAP 3/3). PG17 (khớp managed, lệch plan cũ "15"). uncommitted. |
| **E0a+** | PoC: Auth-hook custom claim + RLS-on-view giữ index | ✅ | claude | ②③ | **DONE 2026-06-29** — `custom_access_token_hook` bơm `tenant_id`/`app_role`/`profile_id`, verified END-TO-END qua GoTrue. **Bắt+fix lỗi:** hook chạy as `supabase_auth_admin` → RLS profiles chặn → claim rỗng (GoTrue log "success"); fix bằng policy `profiles_auth_admin_read`. config.toml enabled. |
| **E0b** | Schema lõi (≈25-30 bảng + tenant_id + enum + 3 ledger + docs + broadcasts) | 🟡 | claude | ①③ | **NON-MONEY DONE** (inc-1→7, 107 test xanh, **23 bảng**): + audit_log, notifications, assignment_rules, leave_requests, staff_details. Batch-3 subagent: review sạch, 0 bug. ⚠️ staff/manager order RLS vẫn = gác (K9). **Còn lại = MONEY CLUSTER (gác③ — dừng xin human liếc):** balances+ledger ×3, payroll, affiliate payout, money-stripped views, 7 test CRITICAL. |
| **E0c** | RLS role+tenant + money-stripped views + docs array-RLS | ⬜ | — | ③ | Chờ E0b, E0a+. **7 test CRITICAL** |
| **E0d** | DB functions (create/advance/cancel/topup) + ledger pattern ×3 | ✅ | claude | ③ | **DONE 2026-06-29** (182 test xanh): inc-1 `topup`+`create_order` atomic · inc-2 `advance_order`+`cancel_order` (planned-only, 5% cancel_fee, refund-to-credit) · inc-3 `post_staff_pay` (+manager override cascade via `staff_details.manager_id`) + `post_affiliate_commission`. Mọi hàm SECURITY DEFINER, invariant balance==SUM(ledger). |

**Cổng mở fleet:** `pnpm verify:db` xanh + 7 test CRITICAL xanh + human duyệt schema/RLS. ✅ **ĐẠT** — **FOUNDATION (E0a, E0a+, E0b, E0d) HOÀN TẤT** (187 pgTAP xanh: cross-tenant=0, money-blind staff/manager=0, atomic balance, balance==SUM(ledger), Auth-hook claim injection verified e2e). → **Fleet Lane A SẴN SÀNG mở.**

**Money cluster (gác③, human duyệt từng increment):**
- ✅ **inc-1 customer_credit** DONE (human duyệt 2026-06-29): `customer_balances` + `credit_ledger`, money-blind (staff=0, manager=0), Stripe idempotency. 3 test CRITICAL xanh.
- ✅ **inc-2 staff_wallet** DONE (human duyệt 2026-06-29): staff_wallet + wallet_ledger + payout_methods + payout_requests, money-blind (manager=0, customer=0). 132 test xanh.
- ✅ **inc-3 affiliate** DONE (human duyệt 2026-06-29): affiliates + referrals + commission + ledger + payouts, money-blind (manager/staff=0) + per-affiliate isolation. **3 hệ tiền XONG.** 146 test xanh.
- 🔵 inc-4 money-stripped view `orders_mgr` (Finding 1) — verified xanh, **chờ human duyệt**
- ✅ **inc-5 manager wallet** (correction): managers CÓ ví riêng (salary+pod commission+payout), thấy mình, money-blind với ví người khác. 156 test xanh.
- ⬜ E0d ledger functions (create_order/topup/advance atomic)

**Frontend bổ sung (ngoài backend):** `/manager/finance` — trang finance riêng cho manager (lương + hoa hồng pod + rút tiền), reuse `FinanceClient`, gate `manager.access` (không phải `finance.view` → tiền pod vẫn ẩn). Verified HTTP 200 + render số thật. Files: `data/managerFinance.ts`, `app/manager/finance/page.tsx`, `managerNav.ts`, `rbac.ts`.

---

## FLEET — song song sau foundation (tối đa 3-4 worktree)

| Lane | Slice | Worktree | Trạng thái | Owner | Gác | Phụ thuộc |
|---|---|---|---|---|---|---|
| **A** | Orders: auth session + **viết lại tầng đọc (orders)** + 1 vòng đơn e2e + wire money-stripped view | `wt-orders` | 🟡 inc-2 | claude | ②③ | E0d, E0c — **inc-1 DONE** (Supabase client + types). **inc-2 DONE (auth session)**: 2a `handle_new_user` trigger (shadow-claim + forced-customer self-signup, gác③ duyệt) · 2b seed 5 demo accounts (demo1234) · 2c/2d login/signup→Supabase Auth + session swap (`useSession` real, `getServerSession` cho RSC) + middleware. **Verified e2e: 5/5 persona login + claim inject + route đúng.** **inc-3a/3b DONE**: `getOrders()`+`getOrderById()` RLS-scoped (`data/orders.server.ts`) + seed 6 customers/11 orders; `/admin/orders` (list+detail), `/admin/orders/[id]`, `/admin` Needs-Attention đọc thật (admin=11, customer=3 own→detail RLS-blocked cho đơn khác, staff=0 money-blind); `buildOrderDetailProps` nhận AdminOrder thật. 194 pgTAP. **inc-3c DONE**: `/manager/orders` + detail đọc `orders_mgr` money-blind (`getPodOrders`/`getPodOrderById`, value-stripped verified; bỏ mock managerScope/MANAGER_PERSONA). **inc-4a/b/c DONE** (writes thật): advance (`advanceOrderAction`) wired ở /admin/orders/[id] + slide-overs; **cancel (`cancelOrderAction`, money gác③, commits `7cd438d`+`9c39e41`)** trên mọi màn admin (hoàn 95% + phí 5%); **seed chạy tiền qua topup+create_order** → 6 khách khớp sổ, huỷ đơn seed reconcile. 201 pgTAP. **inc-3d/e DONE**: customer `/dashboard` thật (board + KPI, `getMyOrders` derive). **inc-3f DONE** (`253fd53`): `/admin/customers` thật (`getCustomers`, không cần seed mới). Còn: **inc-3** assignment/review/staff (assignment+review đã REAL; core task management = task=order, DONE). **Chủ ý: task = order bây giờ, subtask để SAU.** Phần phụ trợ quanh 1 đơn còn mock (optional polish, KHÔNG chặn): deliverable-submit (staff upload), messages trong đơn, My Day, staff perf — cần bảng phụ (perf/messages) hoặc nối bảng deliverables đã có · projects/OrdersSummary (derive) · **inc-4**: `create_order`/`topup` từ UI (Lane B ✅). **Lane A cleanup DONE** (`cc909cd`): **staff My Tasks board reads REAL assigned orders** (money-blind) — `getMyTasks()` (`data/staffTasks.server.ts`) via `orders_mgr` (WHERE gates staff→assignee_id=self + omits value) → StaffTask board; `/staff/tasks` wired (mock fallback for impersonation); seed assigns BL-1006 to Mai. Verified: Mai board=5 real orders money-blind (value omitted + base table blocked); staff `advance_order` assigned→in_progress ok (claims-derived, ownership NOT_YOUR_ORDER). **#1+#3 DONE** (`bd8d5d3`): **admin assign-write + staff order transitions real.** `assign_order(order,staff)` fn (admin-gated, claims-derived; confirmed→assigned + set assignee_id; NOT_STAFF/ORDER_CLOSED guards; pgTAP 0380) + `assignOrderAction` + AssignmentClient persists every path (manual/bulk/auto/rebalance via name→profile id). Staff transitions: `getMyTaskById` + staff task-detail page passes real task+flag; `TaskDetailClient` transition()/submit() → `advanceOrderAction` (advance_order, ownership-enforced) when real; collab panels stay mock. **Verified e2e (full loop):** admin assign KW-1007 confirmed→assigned+Mai → Mai sees money-blind → assigned→in_progress→internal_review → →approved blocked (ILLEGAL_TRANSITION) → non-admin assign blocked. 331 pgTAP + 378 app tests. **Lane A order lifecycle admin→staff→review now real (task=order — core task mgmt DONE).** **Optional polish (không chặn; task=order giờ, subtask sau):** deliverable-submit write + task messages + My Day + staff perf (cần bảng phụ perf/messages, hoặc nối bảng deliverables đã có). |
| **B** | customer_credit ledger UI + quick checkout (6 chốt) + Stripe | `wt-credit` | ✅ | claude | ②③ | **DONE.** **inc-B1** `/credit` đọc balance+ledger thật (`getMyCredit`). **inc-B2** pure `computeOrderPrice` (pricing engine, 9 test). **inc-B3** create-order-from-UI: `placeOrderAction` server-priced → `create_order` qua service-role client → trừ credit + order_details/order_addons. **Phase 2:** simulated-Stripe **top-up** (provider seam `lib/payments/provider.ts` mock→Stripe; `topUpAction`) + **invoices** thật (RLS money-blind, pgTAP 0270). **Quick-checkout 6 chốt** (marketing → dashboard): catalog dời sang `@heva/catalog/orders` (single source) · `priceQuickOrder` · `POST /api/public/checkout` (6 chốt, mock gateway) · `materialize_order` atomic (idempotent `orders.checkout_ref`, pgTAP 0280) · temp-password account + email-collision guard · `customers.billing` save. UI pay-first 2 bước (3-cột: rail · payment · order-details) + confetti + Order-received inline. **Verified e2e in browser.** Migrations `20260630130000`–`170000`. **✅ Stripe TEST mode wired+verified** (`StripeProvider` via pm_card_visa test token; `PAYMENTS_PROVIDER=stripe` + `sk_test` in gitignored `.env.local`; verified real: quick-checkout QO-3104 checkout_ref=pi_…, top-up $80 invoice HD-2026-001 provider_ref=pi_…). Còn deferred: Stripe webhook backstop + client Elements + reconcile (chốt⑥) + email tables (`send_order_email`) + Turnstile key (chốt②). |
| **C** | Docs array-RLS + Broadcasts event log + analytics aggregation | `wt-content` | ✅ inc-C6 | claude | ②③ | E0c — **inc-C1 DONE** (`05c989a`): **customer docs read real (array-RLS)**. `getDocs()` (`data/docs.server.ts`) reads the `docs` table array-RLS-scoped to the viewer's role (admin all; customer/manager by audience; staff by audience+skill-gate) → `StaffDoc[]`, no client filtering. body jsonb carries rich metadata; top-level `audiences[]`/`required_skills[]` drive RLS. `DocsLibrary` takes an optional real `docs` prop (used as-is, else mock fallback); customer `/docs` wired; scope banner gated to staff only. Seed: 2 customer + 1 staff/manager + 1 skill-gated(backlink) docs. **Verified e2e:** customer sees exactly 2 customer docs, staff/manager see staff/manager doc, backlink SOP hidden from Mai (skills claim not injected). **inc-C2 DONE** (`b383b20`): **staff skill-gating via JWT + staff/manager docs real**. `custom_access_token_hook` (migration `20260630230000`) now injects `skills` (text[] from staff_details) into JWT claims → docs RLS skill-gate (`current_skills()`) works e2e (was always {} → all gated docs invisible). Grant + read-all policy for supabase_auth_admin on staff_details. `/staff/docs` + `/manager/docs` read getDocs(); pgTAP 0230 +1 (skills). Seed: keyword-gated doc added. **Verified e2e:** Mai JWT skills=[keyword,optimize,content] → sees keyword playbook + ungated handbook, backlink SOP hidden. 280 pgTAP. **inc-C3 DONE** (`18d0264`): **admin docs authoring (real CRUD) + all doc detail pages real**. `upsert_doc`/`delete_doc` DB fns (admin-gated, claims-derived tenant+author; docs SELECT-only via RLS → writes via fns; tenant-scoped; audit; pgTAP 0350) + `saveDocAction`/`deleteDocAction` + `lib/docAudienceMap` (composer role+skill audience model ↔ DB audiences[]/required_skills[]). AdminDocsManager lists real docs + unpublish; DocComposer create/edit→saveDocAction (HTML sanitized at boundary); getDocs returns required_skills + composer audiences. All 4 doc DETAIL pages now render real via getDocs+DocArticle (were mock → real docs 404'd on click). **Verified e2e:** admin create→Jane sees→retarget to staff→RLS re-gates→delete→gone; non-admin blocked; admin list shows 5 docs w/ correct audience badges. **306 pgTAP + 378 app tests. ✅ Docs feature fully real (read all surfaces + skill-gate + admin CRUD).** **inc-C4 DONE** (`529e15e`): **recipient broadcasts read real** (inbox + bell + banner + site-alert, all 4 portals). `broadcasts_rich` migration (additive cols banner/pinned/cta/article/expires_at/require_ack/display_kind so the table carries the UI model; RLS unchanged). `getMyBroadcasts()` (array-RLS to viewer's role audience; recalled/draft filtered) → `BroadcastProvider` in all 4 portal layouts → `broadcastStore` recipient hooks (useInbox/useBanners/useSiteAlerts) source real-or-mock. Seed 6 broadcasts. **Verified e2e:** RLS per role (customer 3/staff·mgr 2/admin 5, recalled hidden, no leak); customer inbox shows correct tones+CTA+pinned, bell 3 unread, maintenance in site-alert. Read/dismiss/ack still client-side. **306 pgTAP + 378 app tests.** **inc-C5 DONE** (`954427f`): **admin broadcast authoring real** (compose/send/edit/recall/restore/delete). `upsert_broadcast`/`set_broadcast_status`/`delete_broadcast` DB fns (admin-gated, claims-derived tenant+author; broadcasts SELECT-only via RLS; tenant-scoped; audit; pgTAP 0360). `getBroadcasts()` admin reader (all incl recalled) + 3 actions + `BroadcastsManager`/composer/detail wired. **Verified e2e:** admin composes customer broadcast w/ CTA → Jane sees it → recall → Jane loses it (admin still sees) → delete → gone; non-admin blocked; admin console 6 broadcasts w/ KPIs. **316 pgTAP + 378 app tests. Broadcast compose+deliver loop real.** **inc-C6 DONE** (`c336cdb`): **real read-receipts + analytics**. `mark_broadcast_read`/`mark_broadcast_click` fns (claims-derived, recipient-only, idempotent; broadcast_events SELECT-only via RLS; pgTAP 0370). Recipient unread real (getMyBroadcastReadIds + BroadcastProvider readIds + optimistic; bell/inbox/CTA-click → DB; dismiss/ack stay local). Analytics real: getBroadcastAnalytics(id) builds RecEvent[] from real roster+events → existing pure aggregators; getBroadcastReadCounts → admin list. **Verified e2e:** mark-read 0→1 idempotent, non-recipient blocked, admin detail shows real Delivered/Read/CTA/funnel/by-audience, inbox unread 3→0 after reading. **323 pgTAP + 378 app tests.** **✅ LANE C DONE** — docs (C1-C3: array-RLS read all surfaces + skill-gate + admin CRUD) + broadcasts (C4-C6: recipient read + admin authoring + receipts + analytics) all real. Note: the standalone admin **/analytics dashboard** (orders/revenue charts) is a separate surface, may still be mock. (Cleanup done: `DocReaderClient` removed.) |
| **D** | staff_wallet + payroll (base/gig/commission/bonus−penalty, overrides, presets) | `wt-payroll` | ✅ inc-D7 | claude | ②③ | E0d — **inc-D1** (`f311d54`): seed commission via real `post_staff_pay` + `getMyStaffWallet()` reader (RLS money-blind). **inc-D2** (`efda87f`): wired `/staff/finance` to real wallet (`realWallet` prop overrides balance/activity; demo/impersonation→mock fallback; fixed white-on-white wallet card). **inc-D3 DONE** (`0584eaa`): **payout request (write)** — `request_payout(amount, method)` DB fn (claims-derived + own-wallet-only → granted authenticated; atomic debit + `payout_requests` row, guards $50-min/sufficiency/method-ownership; pgTAP 0290) + `getMyStaffWallet` returns real methods+payouts + `requestPayoutAction` + FinanceClient wired (optimistic debit). **Verified e2e (Mai):** request $50 → $77.40→$27.40, 'requested' row w/ real PayPal method, invariant holds. **inc-D4 DONE** (`24ecc40`): **admin resolve payout** — `resolve_payout(request, action)` DB fn (admin-gated claims, atomic; approve/pay; **reject = refund to wallet**; idempotent; pgTAP 0300) + `getPayoutRequests()` admin reader + `resolvePayoutAction` + `WithdrawalRequests` panel atop /admin/finance Payouts tab (Approve/Mark-paid/Reject). **Verified e2e:** Mai requests → admin rejects → wallet refunded 27.40→77.40 (invariant holds); non-admin blocked. 260 pgTAP + 378 app tests. **inc-D5 DONE** (`14380e4`): **penalties** — `staff_penalties` table + `apply_penalty` (admin→debit), `dispute_penalty` (worker→disputed), `waive_penalty` (admin→refund) DB fns (atomic, money-blind RLS, pgTAP 0310); `getMyStaffWallet` returns real penalties → staff Penalties tab + dispute write; admin `getPenalties`/`getWalletStaff` + apply/waive actions + `AdminPenalties` panel in /admin/finance Payouts tab. **Verified e2e:** admin apply $20 → Mai 77.40→57.40 → Mai disputes → admin waives → refund 77.40 (invariant holds); non-admin blocked. 270 pgTAP + 378 app tests. **inc-D6 DONE** (`a07f7f2`): **manager finance + manager payouts** — `request_payout` now accepts staff OR manager (was staff-only; NOT_STAFF→NOT_WORKER; pgTAP 0290 +manager case); seed: Mai→Sofia pod link so `post_staff_pay` cascades a pod-override into Sofia's wallet ($11.62); `/manager/finance` reuses `getMyStaffWallet` (manager_wallet RLS) → real balance + 'pod override' activity. **Verified e2e:** Sofia sees real $11.62 override wallet; managers can request payout. **inc-D7 DONE** (`8d50826`): **admin payroll run** — `payroll_runs` table + `run_payroll(staff, period, salary, gig, bonus)` DB fn (admin-gated, **idempotent per worker+period** — never double-pays; records FIXED pay, commission stays in the wallet → no double-count, no wallet mutation; RLS admin+worker-own; pgTAP 0320) + `getPayrollRuns` reader + `runPayrollAction` + `AdminPayroll` panel (run form + history) in /admin/finance Payouts tab. **Verified e2e:** run 2026-06 (1300+11)=$1,311; idempotent re-run → still 1 record; Mai reads own payslip; non-admin blocked. **279 pgTAP + 378 app tests. ✅ LANE D money loop fully real** (wallet read · payout request→admin resolve/refund · penalties apply/dispute/waive · manager finance · payroll run). **POLISH DONE** (`30c2ef6`): real payout-method management — `add_payout_method`/`set_default_payout_method`/`remove_payout_method` DB fns (claims-derived, own-rows-only, granted authenticated; keep exactly-one-default; pgTAP 0330+0340) wired into FinanceClient (add/set-default/remove call the actions for a real wallet; were local-only) + staff **Payslips tab reads real payroll_runs** (getMyStaffWallet.payslips; mock monthly only as fallback). Seed: 2 payroll runs for Mai. **Verified e2e:** Mai adds wise→default flips (exactly one default), customer blocked (NOT_WORKER), payslips show $1,416/$1,348. Reviewed: claims-derived ✓, table SELECT-only ✓, cross-user denied ✓. **296 pgTAP + 378 app tests. Lane D = fully real, nothing mock left.** |
| **E** | affiliate commission/payout + impersonation act-as | `wt-affiliate` | ✅ inc-E4 | claude | ②③ | E0d, E0a+ — **inc-E1 DONE** (`22c464c`): **affiliate dashboard reads real commission data**. `getMyAffiliate()` (`data/affiliate.server.ts`) → own affiliates row + referrals + commission_ledger + balance + payouts (affiliate_*_own RLS) → PortalData; marketing metadata untabled → defaults; null→mock fallback. `/affiliate` (dash) wired. Seed: Jane (`jane@janeseo.com` affiliate login) real — JANESEO/gold, 3 referrals (vol 1200), ledger +120+80+40−100, balance 140==SUM(ledger), 1 paid payout. **Verified e2e:** Jane real dash (3 referrals, $1200 volume, $240 lifetime commission, invariant holds); staff/customer see 0 (isolated). 331 pgTAP + 378 app tests. **inc-E2 DONE** (`7544393`): **affiliate payout request (write)**. `request_affiliate_payout(amount)` fn (claims-derived own affiliate, atomic debit affiliate_commission + commission_ledger 'payout' + affiliate_payouts 'requested', $50 min, keeps balance==SUM; pgTAP 0390). getMyAffiliate returns authoritative withdrawable balance; `CommissionLedger` Request-payout button wired via `requestAffiliatePayoutAction` (real balance, not cleared-events derivation). **Verified e2e:** Jane request $100 → 140→40 (invariant holds), 'requested' row; re-request INSUFFICIENT_BALANCE; $10 BELOW_MIN; staff NOT_AFFILIATE. 339 pgTAP + 378 app tests. **inc-E3 DONE** (`758966c`): **admin resolve affiliate payout + real directory**. `resolve_affiliate_payout(request, action)` fn (admin-gated, idempotent; approve/pay; reject→refund balance += amount + ledger 'adjustment'; pgTAP 0400) + `getAffiliates()`/`getAffiliatePayouts()` admin readers (aggregate real tables) + `resolveAffiliatePayoutAction`; `AffiliateAdminClient` takes realPartners/realPayouts + resolves via action (effectiveTier derived from volume). **Verified e2e:** reject $60→refund 80→140 (invariant); approve→pay $90→paid (balance unchanged); re-resolve ALREADY_RESOLVED; affiliate NOT_ADMIN. Admin console renders real queue. 347 pgTAP + 378 app tests. **Affiliate money loop closed** (read→request→admin resolve/refund). **inc-E4 DONE** (`e9fe620`): **admin impersonate affiliate reads REAL partner data** — affiliate.server: buildPortal + getAffiliateById (admin RLS any) + getAffiliatePortalData (resolves heva_as_affiliate cookie); Act-as buttons (real uuids since E3) render the partner's real portal. Verified: admin reads any affiliate scoped; affiliate own-only. **✅ LANE E CORE DONE (E1-E4): affiliate read → request → admin resolve/refund → admin act-as, all real.** Optional polish: marketing-metadata tables (platform/niche/clicks defaulted), tier-config write (Rules tab mock), partner status/create write. |

**Thứ tự:** A trước → merge → B/C/D song song → E cuối (nhạy cảm nhất).

---

## Tích hợp (1 coordinator độc quyền)

| Việc | Quy tắc |
|---|---|
| Migration | append-only, serialize qua 1 cửa (tránh đụng timestamp) |
| Types | `supabase gen types` sinh lại sau **mỗi** merge |
| Lane vỡ lúc tích hợp | **quarantine-and-revert**, không patch tại chỗ |
| Sau mỗi merge | chạy lại `pnpm verify:db` trên nhánh tích hợp |

---

## Stop-conditions đang mở (agent dừng, chờ người)

_(trống — chưa chạy)_

| Slice | Vấn đề | Cần gì từ người | Từ khi |
|---|---|---|---|
| — | — | — | — |

> ✅ **RESOLVED 2026-06-29** (commit `2fe9bcf`): E0d write-fn role-forgery đã vá — advance/cancel tự đọc claims + authz + ownership; create_order/topup revoke khỏi anon/authenticated (chỉ service_role). 201 pgTAP, exploit-closed verified e2e. inc-4 writes mở khoá.

---

## Nhật ký gác (human đã quyết gì)

| Ngày | Slice | Kiểu gác | Quyết định |
|---|---|---|---|
| 2026-06-28 | (plan) | — | ADR + ORCHESTRATION + CONTRACTS + STATUS dựng xong; chưa khởi động build |
| 2026-06-29 | A inc-2a | ③ (RLS) | Duyệt `handle_new_user` trigger: self-signup KHÔNG tin metadata role/tenant (forced customer/agency, chống leo thang); shadow-claim giữ role admin-set. Commit `e4d642b`. |
| 2026-06-29 | E0d write-fn harden | ③ (SEC) | Duyệt vá role-forgery: advance/cancel claims-derived + authz; create_order/topup service_role-only. Commit `2fe9bcf`. |

---

## Cách dùng (cho agent)
1. Trước khi bắt đầu slice: đổi hàng của mình sang 🟡, điền Owner.
2. Đụng stop-condition: đổi 🔴, thêm dòng vào "Stop-conditions đang mở".
3. PR xanh: đổi 🔵. Orchestrator gác → ✅ khi merge.
4. **Không** mở lane mới khi cổng fleet chưa ✅.
